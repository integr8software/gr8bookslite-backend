# PHASE 3 — Extract PermissionService (Behavior Preserving)

## IMPORTANT

This project is following an incremental architecture refactor.

DO NOT redesign anything.

DO NOT introduce new behavior.

DO NOT modify any API contracts.

DO NOT change frontend behavior.

DO NOT change database schema.

DO NOT change runtime outputs.

This phase is ONLY about extracting permission computation into its own service.

Everything must continue producing identical outputs.

---

# Current Architecture

Phase 0
✓ Runtime provisioning stabilized.

Phase 1
✓ AccessControlService internally refactored.

Phase 2
✓ EntitlementService extracted.

Current flow:

AccessControlService
│
▼
EntitlementService
│
returns enabled module codes
│
▼
AccessControlService
computes permissions
builds sidebar
returns auth/me

Permission logic is still embedded inside AccessControlService.

That is the next extraction boundary.

---

# Goal

Create a dedicated PermissionService.

After this phase:

AccessControlService
│
▼
EntitlementService
│
▼
PermissionService
│
▼
AccessControlService
│
▼
Sidebar (unchanged)

The frontend must never know this refactor happened.

---

# Current Responsibilities

Move ONLY permission computation.

Examples include:

- company role permissions
- branch role permissions
- membership permissions
- user permission overrides
- admin permissions
- permission aggregation
- permission normalization
- permissionCode:action generation
- duplicate removal

DO NOT move sidebar.

DO NOT move entitlement.

DO NOT move database loading.

---

# New Module

Create

src/common/access/permissions/

containing

PermissionService

PermissionModule

permission.types.ts

permission.service.spec.ts

---

# PermissionService Responsibilities

PermissionService becomes the ONLY place responsible for computing effective permissions.

Example API

computePermissions(...)

returns exactly the same permission array currently returned by AccessControlService.

No behavior changes.

---

# AccessControlService

After extraction it should simply orchestrate.

Pseudo architecture

enabledModules =
entitlementService.getEnabledModules(...)

permissions =
permissionService.computePermissions(...)

sidebar =
existing sidebar logic(...)

return auth payload

No permission logic should remain.

---

# Allowed Refactoring

Move methods only.

Examples

collectPermissions()

mergePermissions()

normalizePermission()

hasPermission()

permission aggregation helpers

admin permission helpers

etc.

Rename private helpers if necessary.

Public behavior MUST remain identical.

---

# Forbidden

DO NOT touch

Sidebar generation

User sidebar customization

Provisioning

Onboarding

Subscription

Billing

Plans

CompanyModule

Entitlement logic

Frontend

Database

Prisma schema

API contracts

Permission semantics

Permission format

Response format

---

# Tests

Create dedicated tests for PermissionService.

Move permission assertions from AccessControlService tests.

Keep all existing tests passing.

No snapshot updates unless absolutely required.

---

# Success Criteria

After this phase

AccessControlService no longer computes permissions.

PermissionService owns permission computation.

AccessControlService only orchestrates.

No frontend changes.

No database changes.

No API changes.

No runtime behavior changes.

No onboarding changes.

No provisioning changes.

Everything passes.

---

# Architecture After Phase 3

AccessControlService
│
├──────────────┐
▼ ▼
EntitlementService PermissionService
│
▼
permissionCode:action[]
│
▼
AccessControlService
│
▼
Existing Sidebar Logic

Exactly the same behavior.

Cleaner boundaries.

---

# Deliverables

Implement the extraction.

Run

npm run typecheck

Run

npm test -- --runInBand

Fix every failing test.

Do not continue into Phase 4.

Stop after PermissionService extraction.

Then provide:

1. Files changed

2. Responsibilities moved

3. Remaining responsibilities inside AccessControlService

4. Architecture diagram after Phase 3

5. Technical debt still remaining

6. Recommendation for Phase 4

Do NOT implement Phase 4.
Stop after Phase 3.
